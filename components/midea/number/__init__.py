# Number sub-component for midea climate.
# All number entities (timer_on, timer_off) are configured under the climate block
# and registered via climate.py's to_code(). This file exists for AUTO_LOAD.
